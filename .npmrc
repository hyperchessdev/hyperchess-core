# Centralized build output — see docs/hyperchess-core-extraction-plan.md §9a.
store-dir=../hyperchess-builds/hyrperchess-core/pnpm-store

# Deliberately NOT pinning `registry=` here: this repo's packages publish to
# the PUBLIC npm registry (see §7/§9b). The private registry
# (https://npm.kyrpy.kyrpy.com, per the kyrpy-npm-repo skill) is used only via
# an explicit --registry flag, as a pre-publish DX-simulation gate — never as
# this repo's default.
