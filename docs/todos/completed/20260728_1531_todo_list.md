# Task: HyperChess Core — Documentation Overhaul & Repository Recreation

## Todo Checklist
- [x] Step 1: Survey current repo state, existing (codex) docs, real crate/package structure and algorithm inventory
- [x] Step 2: Discover local `main` was stale vs. the already-merged PR #1 on GitHub; reconcile onto the real `github/main` (with explicit user sign-off)
- [x] Step 3: Author new top-level docs additive to the merged foundation (GOVERNANCE.md, MAINTAINERS.md; small targeted edits to CITATION.cff/ACKNOWLEDGMENTS.md/README.md/SECURITY.md)
- [x] Step 4: Author new docs/ files (ATTRIBUTION-AND-TRUST, ARCHITECTURE, FORMATS, IDENTITY-PIECES, PROJECT-STORY, GETTING-STARTED, CLI, UCI, ROADMAP, FAQ, FILE-HEADER-CONVENTION) cross-referencing the existing hyperchess-laws.md / search-architecture.md rather than duplicating them
- [x] Step 5: Define and apply SPDX-style versioned file-header convention (HyperChess Developer Team, GPL-3.0-or-later) across all 148 Rust + TypeScript source files
- [x] Step 6: Fill genuine rustdoc/TSDoc gaps via two background agents (rust-doc-sweep, ts-doc-boost) — lowest-density areas first (packages/store adapters, driver API handlers, etc.)
- [x] Step 7: Fix 2 real pre-existing bugs (duplicate match arms in game.rs) and 2 factual doc errors (`serve` → `api` in README.md/SECURITY.md)
- [x] Step 8: Verify build/test/lint (cargo fmt/build/clippy/test; pnpm build/test) — all clean
- [ ] Step 9: Commit everything to `docs/comprehensive-overhaul` branch, push, open PR against `main` (user confirmed: no repo deletion, normal PR flow)
- [ ] Step 10: Move this todo trio to `docs/todos/completed/` once the PR is opened
- [ ] Step 11: Write final session dev-report update if anything changed after this checkpoint

## Notes
- Original plan (delete + recreate the GitHub repo) was explicitly reversed after discovering
  `main` already had a strong, CI-verified, merged foundation (PR #1) that a prior session's
  codex pass never saw either. See `docs/dev-reports/20260728-164036-dev-report.md` for full detail.
- A live-looking GitLab PAT was found embedded in the `gitlab` git remote URL — flagged to the
  user, not touched.
