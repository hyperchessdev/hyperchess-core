# Task: Make the engine zero-dependency (eval + rules + search default build)

## Todo Checklist
- [x] Step 1: hyperchess-rules — move `rand` to dev-dependencies (test-only usage); drop optional `getrandom`; keep `wasm` feature as documented no-op for compatibility
- [x] Step 2: hyperchess-search — remove `rand`; replace RandomBot and MCTS shuffle with the crate-internal XorShift64* PRNG (deterministic/seedable)
- [x] Step 3: hyperchess-rules — replace `lazy_static` with `std::sync::OnceLock`
- [x] Step 4: hyperchess-rules — replace `bitflags` with a hand-rolled const-friendly newtype
- [x] Step 5: Verify zero external deps via `cargo tree` (default features); fmt/clippy/tests green; wasm-pack build still works (pnpm build+test)
- [x] Step 6: Update docs (READMEs, search-architecture, CHANGELOG) to state the zero-dependency guarantee
- [x] Step 7: Commit, push to PR branch, archive todos
