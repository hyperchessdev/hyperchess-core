[package]
name = "_hyperchess-core-workspace-placeholder"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Temporary — makes the Cargo workspace non-empty for Phase 0. Delete this crate the moment Phase 1 (crates/hyperchess-rules) lands; a workspace with any real member no longer needs it."

[lib]
path = "src/lib.rs"
