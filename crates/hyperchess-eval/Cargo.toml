[package]
name = "hyperchess-eval"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "no_std single-source evaluation math shared by the CPU evaluator and the GPU kernel"

[lib]
# Pure arithmetic, no_std — usable from both the std host crate and the CUDA kernel.
